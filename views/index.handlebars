 <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
<h1 id="maintitle">Eat A Burger</h1>

<p id="subtitle">Let's Grab Some Burgers</p>
<h2 id="quote"><i> "That is a tasty Burger!" - Jules Winnfeild</i></h2>
<hr>
<div class="row">
  <div class="col-lg-6">
        <ul>
        {{#each burgers}}
        <li>
        <p><span class="label">ID:</span> {{this.id}}</p>
        <p id="namelbl"><span class="label">Name:</span> <font size="12"> {{this.name}} </font></p>

        <form action="/{{this.id}}?_method=DELETE" method="POST">
          <input type="hidden" name="id" value="{{this.id}}">
          <button type="submit" class="btn btn-danger">Eat This Burger</button>
        </form>

       <button class="btn btn-default"> <a href="/{{this.id}}">Update this Burger</a> </button>
        </li>
        {{/each}}
        </ul>
  </div>
  <div class="col-md 4">
    <h2>Add a Burger</h2>

    <form class="create-form" action="/" method="POST">

      <div class="form-group">
        <label for="auth">Burger:</label>
        <input type="text" id="auth" name="burger">
      </div>

      <button type="submit" class="btn btn-default">Add</button>
    </form>
</div>
  <div class="col-lg-2">
  </div>
</div>
<style type="text/css">
 #maintitle{
  color: yellow;
   text-align: center;
}
#subtitle{
  color:yellow;
   text-align: center;
}
#quote{
  color:red;
  text-align: center;
}
body{
  color:white;
  background-color: black
}
head{
  background-color: black;
} 
#namelnl{
  font-size: large;
  font-weight: bold;
}
ul {
  list-style-type: none;
}

</style>

